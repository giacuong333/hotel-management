import React, { useState, useEffect, useRef } from 'react';

import axios from 'axios';
import { Link, useNavigate } from 'react-router-dom';
import { Colors } from 'chart.js';

const Contacts = () => {
    return (
        <div>
           <div className="elemental"></div>
        </div>
    );
};

export default Contacts;
