export const convertByteArrayToBase64 = (byteArray) => {
    return `data:image/jpeg;base64,${byteArray}`;
};

export const convertByteArrayToBase64List = (byteArrayList) => {
    const length = byteArrayList.length;

    // return `data:image/jpeg;base64,${byteArray}`;
};
